# TARGET 2026-09-18

Did I. The unanswered question for this window. Hit DID.

Prompt file (do not wait for a paste):
`C:\Users\aaron\OneDrive\Documents\prompts\other prompts\did-i-prompt.md`

Ship later, only when he asks:
`C:\Users\aaron\OneDrive\Documents\prompts\multi-session-build-prompt-react.md`

Kind: completion log. Not a calendar. Not a habit RPG. Not a
week of chores. Not Expires with new paint.

Local URL: http://127.0.0.1:48741/
Repo folder: `C:\Users\aaron\Documents\did-i`
Port: 48741

Pages (site letters later):
- Door `#/`
- Name `#/name`
- How `#/read`
- Tonight `#/tonight`
Session 1 is the board only, mounted from `src/App.jsx`.

Auth: none.

Sample: Hayes Street, apt 3, in `src/lib/sample-board.js`.
Fake names. Email on `.example` when copy needs one.

## Search (session 1)

Phrases a stranger would type:

1. “did I take my meds”
2. “lock the door reminder”
3. “habit tracker no account”

What the first page actually was (2026-09-18):

1. Phone pill apps. TakeYourPills, PillTaken, DoseMed. Pills
   only. Store downloads. Not a keep-open page for mixed life.
2. Store apps named Did I Lock It / Did I Lock Up. Checklists,
   ads, photo proof. Not monthly bills plus pills plus a
   one-time mailing on one board.
3. Browser habit pages (looplog, habitstreak, utilokit).
   Checklists and heatmaps. Streaks. Skip is missing or it
   reads as a miss.

Gap: no login, my data, one question on the face, skip that
does not smash you, mixed cadence, JSON, drop `src/lib/` into
a React app you already have.

## Session plan

- [x] Session 1: scaffold, Hayes Street, face, DID / skip,
      hang daily, miss, JSON, persist, hall CSS.
- [ ] A. Cadence. Weekly, monthly, once, every_n. Misses.
- [ ] B. askAfter.
- [ ] C. Catch-up last night.
- [ ] D. Detail. Night strip, edit, duplicate, archive.
- [ ] E. All-clear and empty hall polish.
- [ ] F. Achievements. quietMode.
- [ ] G. Keyboard polish if anything is still dead.
- [ ] H. Long board filter / slot groups.
- [ ] I. Print slip or log why JSON is enough.
- [ ] J. Application CSS. 390. Reduced motion. Draft check.
- [ ] K. Old JSON still loads after a new field.
- [ ] S-0 through S-5. Hash site.
- [ ] C. Copy.
- [ ] M. Motion.
- [ ] Session ship: only when he asks.

## This session

Session 1. Running hallway. Face. DID. skip. Hang daily.

## Next session

A. Weekly, monthly, once, every_n hangs and misses. Face only
shows due tonight (sample already uses those cadences).

## What SHIPPED means

Screenshots, demo video, README Demo, LinkedIn draft, Pages,
hub, shipped-kinds. Only when he asks. This prompt does not
mark SHIPPED.

## Go deep (done-means)

A person can see tonight’s question, DID / skip / undo, hang
a daily lamp, miss on a blank question, download JSON, keep
work after refresh. Host apps get `value` / `onChange`.

## Usefulness (draft)

1. Who else? Anyone who asks “did I lock it” without another
   login.
2. Their data? Lamps through `value` or JSON.
3. Make it theirs? Title, questions, `did-i.css`.
4. Take it? Copy `src/lib/`.
5. No account? Yes.
6. Coworker test? Zip `src/lib/`, import `Tonight`.
7. Keep a copy? JSON download.
8. Miss and recover? Blank question, bad JSON, empty hall.
9. README says how? Session 1: who, run, URL. Copy-folder
   steps stay thin until later letters.
